# Default values for app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 5

image:
  repository: quay.io/acidonpe/jump-app-back-golang
  pullPolicy: IfNotPresent
  tag: latest

service:
  type: ClusterIP
  port: 80
